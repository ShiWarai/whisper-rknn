# docker compose подхватывает переменные из .env (см. docker-compose.yml)
# Единственное место с путём к папке моделей на хосте (volume + лог при старте API):
WHISPER_MODELS_DIR=/mnt/nvme0/models/whisper-rknn-turbo
# Обязательно, если имена rknn без tiny/base/.../turbo в пути (например encoder.rknn)
WHISPER_MODEL_PROFILE=turbo

# API только во внешней сети Docker: http://whisper-rknn-api:8080
# Создание сети: docker network create whisper_rknn_default
# Для интеграции с ботом на другом порту: PORT=9003 в docker-compose.yml environment
