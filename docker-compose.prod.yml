# Продакшен: готовый образ из GHCR (тег :main).
# Локальная сборка отключена — только pull.
#
# docker pull ghcr.io/shiwarai/whisper-rknn:main
# docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# Публикуется автоматически после успешного Deploy на ветке main (publish.yml).
# См. docs/cicd.md — раздел «Prod vs prerelease».

services:
  whisper-rknn:
    image: ghcr.io/shiwarai/whisper-rknn:main
    build: !override null
