# Runtime (linux/arm64 Docker image)
numpy>=1.26,<2
soundfile>=0.12
kaldi_native_fbank>=1.22

# HTTP API
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12

# rknn_toolkit_lite2 ставится в Dockerfile из third_party/*.whl (не в PyPI).
